

import 'package:flutter/material.dart';

class PayPal_item extends StatelessWidget
{
  const PayPal_item({super.key});

  @override
  Widget build(BuildContext context) {
   return const Column(
     children: [
       
     ],
   );
    throw UnimplementedError();
  }
  
}